{
  "name": "flask-todo",
  "version": "1.0.0",
  "description": "A full CRUD todo application with Flask backend and React frontend. Uses layered architecture — route handlers delegate to service logic, which manages model and database operations — keeping each layer focused and testable.",
  "main": "backend/app.py",
  "scripts": {
    "start-backend": "cd backend && python app.py",
    "start-frontend": "cd frontend && npm start",
    "install-all": "pip install -r backend/requirements.txt && cd frontend && npm install"
  },
  "keywords": ["flask", "react", "todo", "layered", "api"],
  "license": "MIT",
  "dependencies": {}
}